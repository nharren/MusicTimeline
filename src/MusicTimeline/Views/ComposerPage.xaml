<Page x:Class="NathanHarrenstein.MusicTimeline.Views.ComposerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:c="clr-namespace:NathanHarrenstein.MusicTimeline.Controls"
      xmlns:cv="clr-namespace:NathanHarrenstein.MusicTimeline.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:l="clr-namespace:NathanHarrenstein.MusicTimeline"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:v="clr-namespace:NathanHarrenstein.MusicTimeline.Views"
      xmlns:db="clr-namespace:NathanHarrenstein.MusicDB;assembly=MusicDB"
      Title="ComposerPage"
      Background="{StaticResource BackgroundBrush}"
      FontFamily="Cambria"
      Foreground="#FFDFE0E6"
      UseLayoutRounding="True"
      d:DesignHeight="600"
      d:DesignWidth="800"
      mc:Ignorable="d">
    <Page.Resources>
        <cv:BytesToImageConverter x:Key="BytesToImageConverter" />
        <cv:NationalityToImageConverter x:Key="NationalityToImageConverter" />
        <cv:UrlToCommandConverter x:Key="UrlToCommandConverter" />
        <cv:UrlToIconConverter x:Key="UrlToIconConverter" />
        <cv:UrlToTitleConverter x:Key="UrlToTitleConverter" />
    </Page.Resources>
    <!--  Root  -->
    <Grid>
        <Grid.Background>
                <ImageBrush ImageSource="pack://application:,,,/Resources/Background/NoiseOverlay.png"
                            Opacity="0.25"
                            TileMode="Tile"
                            Viewport="0, 0, 325, 168"
                            ViewportUnits="Absolute" />
            </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="120" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!--  Header  -->
        <StackPanel Orientation="Horizontal">
            <Grid Width="120" Height="120">
                <Button Margin="0,-3,0,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Click="BackButton_Click"
                        Content="&#xE2A4;"
                        Cursor="Hand"
                        FontFamily="Segoe UI Symbol"
                        FontSize="43"
                        Foreground="#FFDFE0E6">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Grid Margin="{TemplateBinding Margin}">
                                <ContentPresenter />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            <!--  Header Text  -->
            <TextBlock Name="ComposerNameTextBlock" 
                       Margin="0,-1,0,0"
                       VerticalAlignment="Center"
                       FontSize="38"
/>
            <!--  Flag  -->
            <ItemsControl Name="ComposerFlagsItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Height="120" Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type db:Nationality}">
                        <Image Width="32"
                                   Height="32"
                               Margin="15,0,0,0"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Source="{Binding Name, Converter={StaticResource NationalityToImageConverter}}"
                                   ToolTip="{Binding Name}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
        </StackPanel>
        <!--  Body  -->
        <ScrollViewer Grid.Row="1">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar">
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Duration="0:0:0.25"
                                                     From="0.15"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="1" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Duration="0:0:0.25"
                                                     From="1"
                                                     Storyboard.TargetProperty="Opacity"
                                                     To="0.15" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                    <Setter Property="Opacity" Value="0.15" />
                </Style>
            </ScrollViewer.Resources>
            <Grid Margin="30,5,30,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <!--  Left Column  -->
                <StackPanel Grid.Column="0">
                    <!--  Images  -->
                    <ListBox Name="ComposerImagesListBox"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Top"
                             SelectedIndex="0">
                        <ListBox.Template>
                            <ControlTemplate TargetType="{x:Type ListBox}">
                                <Grid>
                                    <Image Name="selectedImage" 
                                           DataContext="{TemplateBinding SelectedItem}"
                                           Source="{Binding Image,
                                                            Converter={StaticResource BytesToImageConverter}, IsAsync=True}" />
                                    <Border Grid.Row="1"
                                            MinWidth="{Binding ActualWidth,
                                                               ElementName=selectedImage}"
                                            MinHeight="30"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            BorderThickness="0">
                                        <c:PanningScrollViewer>
                                            <ItemsPresenter />
                                        </c:PanningScrollViewer>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </ListBox.Template>
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="0,0,5,0" />
                                <Setter Property="Padding" Value="0" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Image Height="30"
                                       MaxWidth="30"
                                       Cursor="Hand"
                                       Source="{Binding Image,
                                                        Converter={StaticResource BytesToImageConverter}}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <!--  Details  -->
                    <StackPanel Margin="15,0,0,0">
                        <!--  Born  -->
                        <TextBlock Margin="0,15,0,5"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Text="Born" />
                        <TextBlock Name="BornTextBlock" FontSize="14" />
                        <!--  Died  -->
                        <TextBlock Margin="0,15,0,5"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Text="Died" />
                        <TextBlock Name="DiedTextBlock" FontSize="14" />
                        <!--  Influences  -->
                        <TextBlock Name="InfluencesTextBlock" 
                                   Margin="0,15,0,3"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Text="Influences" />
                        <ItemsControl Name="InfluencesItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type db:Composer}">
                                    <Button Margin="0,0,0,2"
                                            Click="ComposerButton_Click"
                                            Content="{Binding Name}"
                                            FontSize="14"
                                            Cursor="Hand"
                                            Foreground="White">
                                        <Button.Template>
                                            <ControlTemplate TargetType="{x:Type Button}">
                                                <Grid>
                                                    <ContentPresenter />
                                                </Grid>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!--  Influenced  -->
                        <TextBlock Name="InfluencedTextBlock" 
                                   Margin="0,15,0,3"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Text="Influenced" />
                        <ItemsControl Name="InfluencedItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type db:Composer}">
                                    <Button Margin="0,0,0,2"
                                            Click="ComposerButton_Click"
                                            Content="{Binding Name}"
                                            Cursor="Hand"
                                            FontSize="14"
                                            Foreground="White">
                                        <Button.Template>
                                            <ControlTemplate TargetType="{x:Type Button}">
                                                <Grid>
                                                    <ContentPresenter />
                                                </Grid>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <!--  Links  -->
                        <TextBlock Margin="0,15,0,5"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Text="Links"
                                   Name="LinksTextBlock" />
                        <ItemsControl MinWidth="16"
                                      MinHeight="16"
                                      Name="LinksItemControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type db:ComposerLink}">
                                    <Button Command="{Binding URL, Converter={StaticResource UrlToCommandConverter}}">
                                        <Button.Template>
                                            <ControlTemplate>
                                                <Grid>
                                                    <Image Width="16"
                                                           Height="16"
                                                           Margin="0,0,10,0"
                                                           Cursor="Hand"
                                                           SnapsToDevicePixels="True"
                                                           Source="{Binding URL, Converter={StaticResource UrlToIconConverter}}"
                                                           ToolTip="{Binding URL, Converter={StaticResource UrlToTitleConverter}}"
                                                           UseLayoutRounding="True" />
                                                </Grid>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
                <!--  Center Column  -->
                <StackPanel Grid.Column="1" Margin="30,0,30,0">
                    <!--  Biography  -->
                    <TextBlock Margin="0,0,0,8"
                               FontSize="18"
                               FontWeight="Bold"
                               Text="Biography" />
                    <TextBlock Name="BiographyTextBlock"
                               FontSize="16"
                               TextWrapping="Wrap" />
                </StackPanel>
                <!--  Right Column  -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <!--  Compositions  -->
                    <TextBlock Margin="0,0,0,4"
                               FontSize="18"
                               FontWeight="Bold"
                               Text="Compositions" />
                    <c:TreeView x:Name="TreeView" Grid.Row="1"
                                FontFamily="Cambria"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Foreground="#FFDFE0E6" />
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>